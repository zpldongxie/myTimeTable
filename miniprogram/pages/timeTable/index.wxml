<!--pages/timeTable/index.wxml-->
<view class="container">
  <view class="top">
    <view class="title">{{currentGrade.name}}{{currentClass.name}}课程表</view>
    <view class="subTitle">{{currentSchool.name}}</view>
  </view>
  <view wx:if="{{isReday && schedule}}">有课表</view>
  <view wx:if="{{isReday && !schedule}}" class="subCon">
    <view>这个班级还没有课表</view>
    <view>我来
      <getUserInfoBtn text="创建一个" className="a" force failTips="不告诉我你是谁，就不让你去创建课表哦。" bind:callback="jumpPage" data-page="editSchedule" />
    </view>
    <view>
      <button class="share a" open-type="share">邀请好友</button> 来创建
    </view>
  </view>
</view>